# Nexis Corpus Builder

Python script to convert Nexis DOCX files into cleaned XML files for corpus analysis.

The script extracts metadata, removes boilerplate, normalizes text, classifies articles by section, and exports XML files into category folders.

Input DOCX files and generated corpus files are intentionally ignored by Git.
